# @grade-stack/core

Shared spine: the **provider abstraction** through which *every* model call flows
(reference agent now, eval judges in 1A, the gateway seam in 2C), plus shared
types. Bedrock and Ollama implementations are selectable via `--provider` /
`RELIABILITY_PROVIDER`.

No reliability tooling lives here yet — just model invocation.
